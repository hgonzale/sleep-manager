#!/bin/sh
set -e
if [ "$1" = "purge" ]; then
    rm -f /etc/nginx/sites-enabled/sleep-manager
    if command -v nginx >/dev/null 2>&1 && systemctl is-active --quiet nginx; then
        systemctl reload nginx || true
    fi
fi
#DEBHELPER#
